{
  "name": "player",
  "version": "1.0.0",
  "description": "a package-manager-for-the-brain content player",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "sh -ac ' . ~/.bpmrc; beefy ./index.js --live --index=build/index.html --cwd=build -- -t envify'"
  },
  "author": "Jan Bölsche",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^9.0.3",
    "stylus": "^0.50.0",
    "jade": "^1.9.2",
    "beefy": "^2.1.3"
  },
  "dependencies": {
    "envify": "^3.4.0",
    "events": "^1.0.2",
    "moment": "^2.9.0"
  }
}
